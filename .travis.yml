language: python

python:
- "3.7"

before_script:
  - pip install scikit-image==0.16.2
  - pip install hyperspy
  - pip install dask
  - pip install pyxem
  - pip install -e .
  - pip install pytest-cov

script:
  - pytest --cov=epsic_tools
